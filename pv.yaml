apiVersion: v1
kind: PersistentVolume
metadata:
  name: samruddhi-pv  
spec:
  capacity:
    storage: 1Gi 
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce 
  persistentVolumeReclaimPolicy: Retain 
  storageClassName: standard  
  hostPath:
    path: /samruddhi_PV_dir 
